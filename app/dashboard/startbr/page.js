import { categoryPage } from "@/app/components/categoryPage";
export default categoryPage({ category: "startbr", icon: "flag", title: "Start BR", subtitle: <>Grant a player permission to run <b>/startbr</b>. Works in any server, applies live, and persists across sessions via the DashboardGrants store.</> });

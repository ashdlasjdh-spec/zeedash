import { categoryPage } from "@/app/components/categoryPage";
export default categoryPage({ category: "shazam", icon: "bolt", title: "Shazam", subtitle: "Grant a Shazam variant to any player. Applies live if they're in-game and persists across sessions (PlayerPerks + shared DB), so it survives universe swaps." });
